import * as React from "react";
import { columnIndexToLabel } from "hot-formula-parser";
var ColumnIndicator = function (_a) {
    var column = _a.column, label = _a.label;
    return (React.createElement("th", { className: "Spreadsheet__header" }, label !== undefined ? label : columnIndexToLabel(String(column))));
};
export default ColumnIndicator;
