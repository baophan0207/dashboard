/** Return whether two given points are the equal */
export function isEqual(source, target) {
    return source.column === target.column && source.row === target.row;
}
/** The origin point in matrices */
export var ORIGIN = { row: 0, column: 0 };
