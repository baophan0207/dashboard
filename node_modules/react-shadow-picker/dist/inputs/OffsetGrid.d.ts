/// <reference types="react" />
interface Offset {
    x: number;
    y: number;
}
interface Props {
    max?: number;
    offset: Offset;
    onChange: (offset: Offset) => void;
}
declare const _default: ({ offset: { x, y }, max, onChange }: Props) => JSX.Element;
export default _default;
